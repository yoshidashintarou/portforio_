<h2>編集画面</h2>


<%= form_with model: @user do |f| %>

  <h4>名前</h4>
  <%= f.text_field :name %>

  <h4>メールアドレス</h4>
  <%= f.text_field :email %>

  <%= f.submit '保存' %>

<% end %>
<%= link_to "戻る", user_path %>
